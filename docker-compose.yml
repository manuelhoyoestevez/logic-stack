version: "2"

# Porbar por qué
# "expression": "return (a | b) -> (p <> b);"
# es distinto a
# "expression": "return (a | b) -> (c <> b);"

services:
  logic-stack-server:
    build: ./server
    image: "mhedev/logic-stack-server:0.0.1"
    container_name: "logic-stack-server"
    hostname: "logic-stack-server"
    environment:
      SERVICE_NAME: "logic-stack-server"
      HOST: 0.0.0.0
      PORT: 8081
    volumes:
      - ./server:/usr/src/app
    ports:
      - "8081:8081"
